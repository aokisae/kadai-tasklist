<p>ログイン完了ユーザ: <%= current_user.name %></p>
  <h1>タスク一覧</h1>
  
  <% @task.each do |task| %>
  <li>タスク<%= link_to task.id, task %> : <%= task.content %></li>
  <li>ステータス<%= link_to task.id, task %> : <%= task.status %></li>
<% end %>
<%= link_to '新規タスク作成', new_task_path %>